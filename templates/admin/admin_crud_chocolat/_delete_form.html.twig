<form method="post" action="{{ path('app_admin_crud_chocolat_delete', {'id': chocolat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chocolat.id) }}">
    <button class="btn btn-danger me-2"><i class="align-middle" data-feather="trash-2"></i></button>
</form>
