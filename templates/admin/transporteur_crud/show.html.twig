{% extends 'base-backend.html.twig' %}


{% block title %}Transporteur{% endblock %}

{% block content %}
    <h1>Transporteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ transporteur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ transporteur.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ transporteur.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ transporteur.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_transporteur_crud_index') }}">back to list</a>

    <a href="{{ path('app_transporteur_crud_edit', {'id': transporteur.id}) }}">edit</a>

    {{ include('admin/transporteur_crud/_delete_form.html.twig') }}
{% endblock %}
