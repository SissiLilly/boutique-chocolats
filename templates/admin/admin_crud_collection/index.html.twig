{% extends 'base-backend.html.twig' %}

{% block title %}Collection index{% endblock %}

{% block content %}
    <h1>Collection index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom_collection</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for collection in collections %}
            <tr>
                <td>{{ collection.id }}</td>
                <td>{{ collection.nomCollection }}</td>
                <td>
                    <a href="{{ path('app_admin_crud_collection_show', {'id': collection.id}) }}">show</a>
                    <a href="{{ path('app_admin_crud_collection_edit', {'id': collection.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_admin_crud_collection_new') }}">Create new</a>
{% endblock %}
