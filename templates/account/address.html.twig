{% extends 'base.html.twig' %}

{% block title %}Mes adresses - Huvelin{% endblock %}

{% block content %}
    <div class="container mt-5 mb-5">
        <a href="{{ path('account_address_add') }}" class="btn btn-info btn-sm float-end">Ajouter une adresse</a>
        <h1 class="mb-4">Mes adresses</h1>
        <p>C'est dans cet espace que vous allez pouvoir gérer toutes vos adresses.</p>
        <a href="{{ path('app_account') }}">Retour</a>
        <hr>
        {% if app.user.adresses|length == 0 %}
            <p class="text-center mt-5 mb-5">
                Vous n'avez pas encore ajouté d'adresse dans votre compte client. Pour en ajouter une, veuillez <a href="{{ path('account_address_add') }}">cliquer ici</a>.
            </p>
        {% else %}
            <div class="row">
                {% for address in app.user.adresses %}
                    <div class="col-md-4 mb-4">
                        <div class="card" style="width: 18rem;">
                            <div class="card-body">
                                <h5 class="card-title">{{ address.pays }}</h5>
                                <p class="card-text">
                                    {{ address.address }},<br>
                                    {{ address.city}} 
                                </p>
                                <div class="d-flex justify-content-between align-items-center">
                                    <a href="{{ path('account_address_edit', { 'id' : address.id }) }}" class="btn btn-primary">Modifier</a>
                                    <a href="{{ path('account_address_delete', { 'id' : address.id }) }}" class="btn btn-danger">Supprimer</a>
                                </div>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% endif %}
    </div>
{% endblock %}
